export interface AdministradorDistribuidor {
    id  : number,
    denominacion: string
}

export interface Cajero{
    id: number,
    cajero: string
}

export interface TipoPago {
    id: number,
    tipopago: string
}

export interface Status {
    id: number,
    status: string
}

export interface Plataforma {
    id: number,
    plataforma: string
}

export interface Concepto {
    id: number,
    denominacion: string
}
